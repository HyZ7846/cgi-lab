1. 
import os
import json

print("Content-Type: application/json")
print()
print(json.dumps(dict(os.environ), indent=2))

2.
"QUERY_STRING"

3.
"HTTP_USER_AGENT"

4.
cgi.FieldStorage()

5.
Set-Cookie: name=value;[options]

6.
The HTTP header syntax that the browser uses to send the cookie back to the server is "Cookie" by a colon and the cookie value.

7. 
cookies is used for storing user data.

8.
